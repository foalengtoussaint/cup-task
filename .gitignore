# weights (copied in, not versioned)
*.pt
*.onnx
# data / clips / outputs
data/clips/
data/calib/
*.mp4
*.pose.json
out/
# datasets + training runs (large, regenerable; kept on disk, not in git)
data/
runs/
# python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/
out*/
